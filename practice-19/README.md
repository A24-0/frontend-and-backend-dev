# Практика 19 — PostgreSQL

CRUD API пользователей с обязательным подключением к PostgreSQL.

## Маршруты
- `POST /api/users`
- `GET /api/users`
- `GET /api/users/:id`
- `PATCH /api/users/:id`
- `DELETE /api/users/:id`

## Запуск
```bash
cp .env.example .env
npm install
npm start
```
