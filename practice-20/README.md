# Практика 20 — MongoDB

CRUD API пользователей, где маршруты полностью связаны с NoSQL БД (MongoDB).

## Маршруты
`POST /api/users`, `GET /api/users`, `GET /api/users/:id`, `PATCH /api/users/:id`, `DELETE /api/users/:id`.
